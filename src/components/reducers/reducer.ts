export type AddNumberCountType = {
    type: 'ADD-NEW-VALUE'
    startValue: number,
    maxValue: number
}

const reducer = (state: any, action: any): any => {
    switch (action.type) {
        case 'ADD-NEW-VALUE':

    }
}